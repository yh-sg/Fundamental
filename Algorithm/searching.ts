const linearSearch = (arr:string[]|number[], val:string|number):string => {
    let result;
    return `Value ${val} was found in position ${result} for searching in ${arr}`
};

const binarySearch = (arr:string[]|number[], val:string|number):string => {
    let result;
    return `Value ${val} was found in position ${result} for searching in ${arr}`
};

const BFS = () => {
    
};

const DFS = () => {
    
};